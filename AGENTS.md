# Docker

/main directory is an orchestrator. Use docker-compose setup from there to run or interact with the app:

```bash
docker-compose -f ~/Repos/mineralogy-rocks/main/docker-compose.yaml up backend
```

## Local Environment

- the site is served at `http://api.mineralogy.rocks.local`
